<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.14</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.dddheroes</groupId>
	<artifactId>heroesofddd</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>heroesofddd</name>
	<description>heroesofddd</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<assertj.version>3.27.7</assertj.version>
		<axon.version>5.3.0-SNAPSHOT</axon.version>
		<axon5.version>5.3.0-SNAPSHOT</axon5.version>
		<axoniq-console.version>1.9.3</axoniq-console.version>
		<datasource-micrometer.version>1.4.1</datasource-micrometer.version>
		<java.version>23</java.version>
		<!-- Aligned to the opentelemetry-api version managed by the Spring Boot 3.5 BOM (1.49.0):
		     instrumentation 2.15.0-alpha ships opentelemetry-api 1.49.0. Bump both together. -->
		<opentelemetry-instrumentation.version>2.15.0-alpha</opentelemetry-instrumentation.version>
		<r2dbc-proxy.version>1.1.6.RELEASE</r2dbc-proxy.version>
		<spring-ai.version>1.0.1</spring-ai.version>
		<spring-doc.version>2.8.17</spring-doc.version>
	</properties>

	<repositories>
		<repository>
			<id>sonatype-snapshots</id>
			<name>Sonatype Snapshots</name>
			<url>https://central.sonatype.com/repository/maven-snapshots/</url>
			<snapshots>
				<enabled>true</enabled>
			</snapshots>
			<releases>
				<enabled>false</enabled>
			</releases>
		</repository>
	</repositories>

	<dependencyManagement>
		<dependencies>
			<!-- AF5's axon-conversion uses Jackson 3 (tools.jackson 3.2.0), which references
			     com.fasterxml.jackson.annotation.JsonApplyView — added in jackson-annotations 2.22.
			     The Jackson 2.x databind (2.21.2) transitively pulls jackson-annotations 2.21, which
			     lacks that class, so a Jackson 3 view-processing path fails at runtime with
			     NoClassDefFoundError (and, via a UnitOfWork quirk, hangs PooledStreamingEventProcessor
			     startup rather than failing fast). Pin 2.22 so both Jackson 2.x and 3.x agree. -->
			<dependency>
				<groupId>com.fasterxml.jackson.core</groupId>
				<artifactId>jackson-annotations</artifactId>
				<version>2.22</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webflux</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-r2dbc</artifactId>
		</dependency>
		<!-- Compile scope (not runtime): the jsonb column converters reference io.r2dbc.postgresql.codec.Json. -->
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>r2dbc-postgresql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>
		<dependency>
			<groupId>io.axoniq.framework</groupId>
			<artifactId>axoniq-spring-boot-starter</artifactId>
			<version>${axon.version}</version>
		</dependency>
		<!-- Native Mono/Flux gateways (ReactorCommandGateway/ReactorQueryGateway/ReactorEventGateway),
		     auto-configured by ReactorAutoConfiguration (shipped with the OSS autoconfigure module that
		     the starter above already pulls in). -->
		<dependency>
			<groupId>org.axonframework.extensions.reactor</groupId>
			<artifactId>axon-reactor</artifactId>
			<version>${axon.version}</version>
		</dependency>

		<!-- Distributed tracing (feature 3594).
		     Tracing ships inside the framework modules themselves: the tracing API and the
		     command/event/query bus decorators live in axon-messaging, repository/state-manager
		     tracing in axon-modelling, event-store/snapshot-store tracing in axon-eventsourcing,
		     and the Axon Server connector-leg tracing in axoniq-distributed-messaging — all already
		     on the classpath via the starter above, each wired by ServiceLoader-discovered
		     enhancers. The only extra dependency needed is the OpenTelemetry SpanFactory binding
		     below; the Spring Boot autoconfig (OSS TracingAutoConfiguration + commercial
		     DistributedTracing/OpenTelemetryTracing autoconfigurations) also comes with the
		     starter. -->
		<dependency>
			<groupId>io.axoniq.framework</groupId>
			<artifactId>axoniq-tracing-micrometer</artifactId>
			<version>${axon.version}</version>
		</dependency>

		<!-- OpenTelemetry export pipeline. Spring Boot's actuator tracing autoconfiguration creates the
		     io.opentelemetry.api.OpenTelemetry SDK bean (configured by the management.tracing.* /
		     management.otlp.tracing.* properties in the 'observability' profiles) and exports spans over
		     OTLP. AxoniqFramework's OpenTelemetryTracingAutoConfiguration consumes that same OpenTelemetry
		     bean, so framework spans and HTTP server spans share one tracer/exporter. Versions are managed
		     by the Spring Boot parent BOM, keeping opentelemetry-api/-sdk aligned across both stacks.
		     Without the 'observability' profile, management.tracing.enabled=false → no OpenTelemetry bean →
		     the factory falls back to a no-op GlobalOpenTelemetry and nothing is exported. -->
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-tracing-bridge-otel</artifactId>
		</dependency>
		<dependency>
			<groupId>io.opentelemetry</groupId>
			<artifactId>opentelemetry-exporter-otlp</artifactId>
		</dependency>

		<!-- JDBC/JPA tracing: wraps the autoconfigured HikariCP DataSource in a proxy that emits a
		     Micrometer Observation per JDBC connection/query. These flow through the same
		     micrometer-tracing-bridge-otel → OTLP pipeline as the Axon and HTTP spans, so every SQL
		     statement (Axon JPA event store + read-model projections) becomes a child span carrying
		     the SQL text (and bind parameters). Runtime gate: jdbc.datasource-proxy.enabled
		     (see application(-observability).yaml). -->
		<dependency>
			<groupId>net.ttddyy.observation</groupId>
			<artifactId>datasource-micrometer-spring-boot</artifactId>
			<version>${datasource-micrometer.version}</version>
		</dependency>
		<!-- R2DBC tracing: r2dbc-proxy on the classpath activates Spring Boot's own
		     R2dbcObservationAutoConfiguration, which wraps the reactive ConnectionFactory with
		     r2dbc-proxy's ObservationProxyExecutionListener. Each reactive SQL execution becomes a
		     Micrometer Observation ("r2dbc.query...") that resolves its parent from the Reactor
		     Context captured at subscription, and flows through the same
		     micrometer-tracing-bridge-otel → OTLP pipeline as everything else. Runtime gate:
		     management.observations.enable.r2dbc (see application(-observability).yaml). -->
		<dependency>
			<groupId>io.r2dbc</groupId>
			<artifactId>r2dbc-proxy</artifactId>
			<version>${r2dbc-proxy.version}</version>
		</dependency>
		<!-- gRPC tracing for the Axon Server connector channel. Provides GrpcTelemetry, whose
		     ClientInterceptor is registered on the connection channel via a ManagedChannelCustomizer
		     bean (see GrpcTracingConfiguration). Built from the same OpenTelemetry SDK bean as the
		     Axon/HTTP/JDBC spans, so gRPC client spans flow through the existing OTLP pipeline.
		     Only relevant under an axonserver profile (Axon Server is disabled by default). -->
		<dependency>
			<groupId>io.opentelemetry.instrumentation</groupId>
			<artifactId>opentelemetry-grpc-1.6</artifactId>
			<version>${opentelemetry-instrumentation.version}</version>
		</dependency>

		<dependency>
			<groupId>org.axonframework.extensions.spring</groupId>
			<artifactId>axon-spring-boot-starter-test</artifactId>
			<version>${axon.version}</version>
			<scope>test</scope>
		</dependency>
<!--		<dependency>-->
<!--			<groupId>io.axoniq.console</groupId>-->
<!--			<artifactId>console-framework-client-spring-boot-starter</artifactId>-->
<!--			<version>${axoniq-console.version}</version>-->
<!--		</dependency>-->

		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
			<version>${spring-doc.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-starter-mcp-server-webflux</artifactId>
			<version>${spring-ai.version}</version>
		</dependency>
		<dependency>
			<groupId>org.jetbrains</groupId>
			<artifactId>annotations</artifactId>
			<version>26.0.2-1</version>
		</dependency>

		<!-- Runtime dependencies -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>io.axoniq.framework</groupId>
			<artifactId>axoniq-testcontainer</artifactId>
			<version>${axon.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

<!-- Test dependencies -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>postgresql</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- R2dbcConnectionDetails for the @ServiceConnection PostgreSQLContainer (reactive read models). -->
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>r2dbc</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.awaitility</groupId>
			<artifactId>awaitility</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.axonframework</groupId>
			<artifactId>axon-test</artifactId>
			<scope>test</scope>
            <version>${axon.version}</version>
		</dependency>
		<dependency>
			<groupId>org.assertj</groupId>
			<artifactId>assertj-core</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>stabilization-WhenWeekStartedThenProclaimWeekSymbolTest</id>
			<dependencyManagement>
				<dependencies>
					<dependency>
						<groupId>com.fasterxml.jackson.core</groupId>
						<artifactId>jackson-annotations</artifactId>
						<version>2.21</version>
					</dependency>
				</dependencies>
			</dependencyManagement>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-compiler-plugin</artifactId>
						<configuration>
							<testIncludes>
								<testInclude>com/dddheroes/heroesofddd/TestcontainersConfiguration.java</testInclude>
								<testInclude>com/dddheroes/heroesofddd/utils/AwaitilityUtils.java</testInclude>
								<testInclude>com/dddheroes/heroesofddd/astrologers/automation/whenweekstartedthenproclaimweeksymbol/WhenWeekStartedThenProclaimWeekSymbolTest.java</testInclude>
							</testIncludes>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
