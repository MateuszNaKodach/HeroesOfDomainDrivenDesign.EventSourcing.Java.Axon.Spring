<!--
  Per-recipe migration profile. Copy as a child of <profiles>...</profiles>
  in the relevant module's pom.xml. Replace placeholders:
    <recipe>     — recipe id (aggregate, event-processor, …)
    <Target>     — simple class name of the migrated target (Calendar, Faculty)
    <relpath>    — repo-relative file paths under src/main/java or src/test/java

  See references/maven-profile/maven-profile.md for usage rules,
  the Recipe-to-deps cheat sheet, and the Jackson 3 / Spring Boot pin.

  Project-level prereq (one-time): add <axon5.version> to root <properties>.
-->
<profile>
    <id>migration-<recipe>-<Target></id>
    <dependencyManagement>
        <dependencies>
            <!-- AF5 deps the recipe needs. List only what the recipe pulls in. -->
            <dependency>
                <groupId>org.axonframework</groupId>
                <artifactId>axon-modelling</artifactId>
                <version>${axon5.version}</version>
            </dependency>
            <dependency>
                <groupId>org.axonframework</groupId>
                <artifactId>axon-eventsourcing</artifactId>
                <version>${axon5.version}</version>
            </dependency>
            <dependency>
                <groupId>org.axonframework</groupId>
                <artifactId>axon-test</artifactId>
                <version>${axon5.version}</version>
            </dependency>

            <!-- Jackson 3 (used by AF5 axon-conversion) requires jackson-annotations 2.21+.
                 Spring Boot 3.5.x pins 2.19.x via its BOM, which breaks AxonTestFixture.
                 Pin here when this profile pulls in axon-test. -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>2.21</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <dependencies>
        <dependency><groupId>org.axonframework</groupId><artifactId>axon-modelling</artifactId></dependency>
        <dependency><groupId>org.axonframework</groupId><artifactId>axon-eventsourcing</artifactId></dependency>
        <dependency><groupId>org.axonframework</groupId><artifactId>axon-test</artifactId><scope>test</scope></dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <includes>
                        <include><relpath under src/main/java>.java</include>
                        <!-- one per main-source file -->
                    </includes>
                    <testIncludes>
                        <testInclude><relpath under src/test/java>.java</testInclude>
                        <!-- one per test-source file -->
                    </testIncludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</profile>
